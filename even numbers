#include<iostream.h>
using namespace std;

int main(){
int num;
cout<<"Enter the number";
cin>>num;

cout<<"Even numbers from 0 to"<<num<<"are = \n";
for(int i = 0; i <= num ; i++){
 if(i % 2 == 0){
  cout<<i<<endl;
 }
}
return 0;
}
